name: mosaic_android
description: "Android AppWidget (RemoteViews/Kotlin) code generator for the Mosaic home-widget framework."
publish_to: none
version: 1.0.0
# TODO: update to the real repo URL
repository: https://github.com/mustafa-707/mosaic-widgets
topics:
  - home-widget
  - android
  - code-generation
  - widget

environment:
  sdk: ">=3.0.0 <4.0.0"

# Add regular dependencies here.
dependencies:
  mosaic_core:
    path: ../core
  path: ^1.8.3

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
